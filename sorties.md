{"mermaid_diagram":"flowchart LR\n    A[Pages] -->|User Interaction| B[Clerk Authentication]\n    B -->|Sign In/Sign Up| C[API Routes]\n    C -->|Database Queries| D[PostgreSQL Database]\n    D -->|Data Retrieval| C\n    C -->|User Data| A\n    A -->|UI Components| E[Tailwind CSS]","requirements":["Modèle Prisma: Post avec champs title, content, slug @unique, published Boolean, authorId String","Page publique: / liste des posts publiés","Page publique: /blog/[slug] affichage article par slug","Page protégée: /dashboard gestion posts auteur","API Route: PUT /api/posts/[id] toggle published avec auth","Feature: slug généré côté serveur depuis title"],"spec_unmatched_requirements":[],"spec_validation_status":"OK","specification":"## Vue d'ensemble\nCette spécification technique décrit une application web construite avec Next.js utilisant Clerk pour l'authentification, Prisma avec PostgreSQL pour la base de données, et Tailwind CSS pour le design. L'application inclut des fonctionnalités de gestion de contenu, permettant aux utilisateurs de créer, lire, mettre à jour et supprimer des articles de blog.\n\n## Stack technique\n- **Framework**: Next.js App Router\n- **Authentification**: Clerk\n- **Base de données**: Prisma + PostgreSQL\n- **UI**: Tailwind CSS\n- **Langage**: TypeScript\n\n## Structure des pages\n### Pages\n- **Page d'accueil**: `/`\n  - Affiche une liste d'articles publiés.\n  \n- **Page de blog**: `/blog/[slug]`\n  - Affiche le contenu d'un article spécifique basé sur le `slug`.\n\n- **Tableau de bord**: `/dashboard`\n  - Permet aux utilisateurs de gérer leurs articles (CRUD).\n\n- **Page de connexion**: `/sign-in`\n  - Permet aux utilisateurs de se connecter.\n\n- **Page d'inscription**: `/sign-up`\n  - Permet aux nouveaux utilisateurs de s'inscrire.\n\n## Schéma Prisma\n### Modèle Post\n```prisma\nmodel Post {\n  id        String   @id @default(cuid())\n  title     String\n  slug      String   @unique\n  published Boolean  @default(false)\n  authorId  String\n  createdAt DateTime @default(now())\n  updatedAt DateTime @updatedAt\n}\n```\n\n## Authentification Clerk\n- Le `ClerkProvider` doit être inclus dans `app/layout.tsx`.\n- Le fichier `middleware.ts` doit contenir `clerkMiddleware()` et `createRouteMatcher`.\n- Les routes publiques sont `/sign-in` et `/sign-up`.\n\n## API Routes\n### Routes API\n- **GET /api/posts**\n  - **Auth requise**: Oui\n  - **Corps de requête**: Aucun\n  - **Réponse**: Liste des articles.\n\n- **POST /api/posts**\n  - **Auth requise**: Oui\n  - **Corps de requête**: \n    ```json\n    {\n      \"title\": \"string\",\n      \"slug\": \"string\",\n      \"published\": \"boolean\"\n    }\n    ```\n  - **Réponse**: Article créé.\n\n- **PUT /api/posts/[id]**\n  - **Auth requise**: Oui\n  - **Corps de requête**: \n    ```json\n    {\n      \"title\": \"string\",\n      \"slug\": \"string\",\n      \"published\": \"boolean\"\n    }\n    ```\n  - **Réponse**: Article mis à jour.\n\n- **DELETE /api/posts/[id]**\n  - **Auth requise**: Oui\n  - **Corps de requête**: Aucun\n  - **Réponse**: Article supprimé.\n\n## Composants Tailwind\n- Utiliser Tailwind CSS pour tous les styles.\n- Les composants doivent être responsives et accessibles.\n- Les classes Tailwind doivent être utilisées pour la mise en page, les boutons, les formulaires, et les typographies."}

{"combined_files":{".env.local":"NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_placeholder\nCLERK_SECRET_KEY=sk_test_placeholder\nDATABASE_URL=postgresql://user:password@localhost:5432/personal-blog\n","app/api/posts/[id]/route.ts":"import { NextResponse } from 'next/server';\nimport { auth } from '@clerk/nextjs/server';\nimport prisma from '@/lib/prisma';\n\nexport async function PUT(request: Request, { params }: { params: { id: string } }) {\n  const { id } = params;\n  const { published } = await request.json();\n\n  const { userId } = await await auth.protect();\n\n  const post = await prisma.post.update({\n    where: { id },\n    data: { published },\n  });\n\n  return NextResponse.json(post);\n}","app/blog/[slug]/page.tsx":"import { useEffect, useState } from 'react';\nimport { useRouter } from 'next/navigation';\n\ninterface Post {\n  id: string;\n  title: string;\n  content: string;\n  slug: string;\n  published: boolean;\n  authorId: string;\n  createdAt: Date;\n  updatedAt: Date;\n} \n\nconst BlogPost = () => {\n  const router = useRouter();\n  const { slug } = router.query;\n  const [post, setPost] = useState<Post | null>(null);\n\n  useEffect(() => {\n    const fetchPost = async () => {\n      if (slug) {\n        const response = await fetch(`/api/posts/${slug}`);\n        const data = await response.json();\n        setPost(data);\n      }\n    };\n    fetchPost();\n  }, [slug]);\n\n  if (!post) return <div>Loading...</div>;\n\n  return (\n    <div>\n      <h1>{post.title}</h1>\n      <p>{post.content}</p>\n    </div>\n  );\n};\n\nexport default BlogPost;","app/dashboard/page.tsx":"import { useEffect, useState } from 'react';\nimport { useAuth } from '@clerk/nextjs';\nimport { useRouter } from 'next/navigation';\n\ninterface Post {\n  id: string;\n  title: string;\n  slug: string;\n  published: boolean;\n  authorId: string;\n  createdAt: Date;\n  updatedAt: Date;\n} \n\nconst Dashboard = () => {\n  const { user } = useAuth();\n  const [posts, setPosts] = useState<Post[]>([]);\n  const router = useRouter();\n\n  useEffect(() => {\n    const fetchPosts = async () => {\n      const response = await fetch('/api/posts');\n      const data = await response.json();\n      setPosts(data);\n    };\n    fetchPosts();\n  }, []);\n\n  const togglePublished = async (id: string) => {\n    await fetch(`/api/posts/${id}`, {\n      method: 'PUT',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ published: !posts.find(post => post.id === id)?.published })\n    });\n    router.reload();\n  };\n\n  return (\n    <div>\n      <h1>Tableau de bord</h1>\n      <ul>\n        {posts.map((post) => (\n          <li key={post.id}>\n            <span>{post.title}</span>\n            <button onClick={() => togglePublished(post.id)}>Toggle Published</button>\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n};\n\nexport default Dashboard;","app/layout.tsx":"import { ClerkProvider } from '@clerk/nextjs';\nexport const dynamic = \"force-dynamic\";\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <ClerkProvider>\n      <html lang=\"en\">\n        <body>{children}</body>\n      </html>\n    </ClerkProvider>\n  );\n}","app/page.tsx":"import { useEffect, useState } from 'react';\nimport { useAuth } from '@clerk/nextjs';\n\ninterface Post {\n  id: string;\n  title: string;\n  slug: string;\n  published: boolean;\n  authorId: string;\n  createdAt: Date;\n  updatedAt: Date;\n} \n\nconst HomePage = () => {\n  const { user } = useAuth();\n  const [posts, setPosts] = useState<Post[]>([]);\n\n  useEffect(() => {\n    const fetchPosts = async () => {\n      const response = await fetch('/api/posts');\n      const data = await response.json();\n      setPosts(data);\n    };\n    fetchPosts();\n  }, []);\n\n  return (\n    <div>\n      <h1>Articles publiés</h1>\n      <ul>\n        {posts.map((post) => (\n          <li key={post.id}>\n            <a href={`/blog/${post.slug}`}>{post.title}</a>\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n};\n\nexport default HomePage;","jest.config.js":"const nextJest = require('next/jest');\nconst createJestConfig = nextJest({ dir: './' });\n\nmodule.exports = createJestConfig({\n  testEnvironment: 'jsdom',\n  setupFilesAfterEnv: ['<rootDir>/jest.setup.js'],\n  moduleNameMapper: {\n    '^@/(.*)$': '<rootDir>/$1',\n  },\n});\n","jest.setup.js":"import '@testing-library/jest-dom';\n","lib/prisma.ts":"import { PrismaClient } from '@prisma/client';\n\nconst prisma = new PrismaClient();\n\nexport { prisma };\nexport default prisma;\n","middleware.ts":"import { clerkMiddleware, createRouteMatcher } from '@clerk/nextjs/server';\n\nconst isPublicRoute = createRouteMatcher(['/sign-in(.*)', '/sign-up(.*)']);\n\nexport default clerkMiddleware(async (auth, req) => {\n  if (!isPublicRoute(req)) await auth.protect();\n});\n\nexport const config = {\n  matcher: [\n    '/((?!_next|[^?]*\\\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)',\n    '/(api|trpc)(.*)',\n  ],\n};\n","next.config.js":"/** @type {import('next').NextConfig} */\nconst nextConfig = {\n  reactStrictMode: true,\n  eslint: {\n    ignoreDuringBuilds: true,\n  },\n  typescript: {\n    ignoreBuildErrors: false,\n  },\n};\nmodule.exports = nextConfig;\n","package.json":"{\n  \"name\": \"personal-blog\",\n  \"version\": \"1.0.0\",\n  \"private\": true,\n  \"scripts\": {\n    \"build\": \"next build\",\n    \"dev\": \"next dev\",\n    \"start\": \"next start\",\n    \"lint\": \"next lint\",\n    \"test\": \"jest\"\n  },\n  \"dependencies\": {\n    \"next\": \"14.2.25\",\n    \"react\": \"^18.2.0\",\n    \"react-dom\": \"^18.2.0\",\n    \"@clerk/nextjs\": \"^6.0.0\",\n    \"prisma\": \"^7.0.0\",\n    \"@prisma/client\": \"^7.0.0\",\n    \"typescript\": \"^5.3.3\"\n  },\n  \"devDependencies\": {\n    \"ts-jest\": \"29.1.2\",\n    \"@types/react\": \"^18.2.0\",\n    \"@types/react-dom\": \"^18.2.0\",\n    \"@types/jest\": \"^29.0.0\",\n    \"@types/node\": \"^20.0.0\",\n    \"jest\": \"^29.0.0\",\n    \"jest-environment-jsdom\": \"^29.0.0\",\n    \"@testing-library/jest-dom\": \"^6.0.0\",\n    \"@testing-library/react\": \"^14.0.0\",\n    \"@babel/runtime\": \"^7.0.0\",\n    \"node-mocks-http\": \"^1.14.0\",\n    \"eslint\": \"^8.0.0\",\n    \"eslint-config-next\": \"14.2.25\"\n  }\n}","prisma.config.ts":"import 'dotenv/config';\nimport { defineConfig, env } from 'prisma/config';\n\nexport default defineConfig({\n  schema: 'prisma/schema.prisma',\n  datasource: { url: env('DATABASE_URL') },\n});","prisma/schema.prisma":"datasource db {\\n  provider = \"postgresql\"\\n  url      = env(\"DATABASE_URL\")\\n}\\n\\ngenerator client {\\n  provider = \"prisma-client-js\"\\n}\\n\\nmodel Post {\\n  id        String   @id @default(cuid())\\n  title     String\\n  content   String\\n  slug      String   @unique\\n  published Boolean  @default(false)\\n  authorId  String\\n  createdAt DateTime @default(now())\\n  updatedAt DateTime @updatedAt\\n}","tests/api/posts.test.ts":"import { NextResponse } from 'next/server';\nimport { PUT } from '../app/api/posts/[id]/route';\nimport { auth } from '@clerk/nextjs/server';\nimport prisma from '@/lib/prisma';\nimport httpMocks from 'node-mocks-http';\n\njest.mock('@clerk/nextjs/server', () => ({ auth: { protect: jest.fn() } }));\njest.mock('@/lib/prisma', () => ({ post: { update: jest.fn() } }));\n\ndescribe('PUT /api/posts/[id]', () => {\n  it('updates a post', async () => {\n    const req = httpMocks.createRequest({\n      method: 'PUT',\n      body: { published: true },\n      params: { id: '1' },\n    });\n    const res = httpMocks.createResponse();\n\n    (auth.protect as jest.Mock).mockResolvedValue({ userId: 'user-id' });\n    (prisma.post.update as jest.Mock).mockResolvedValue({ id: '1', published: true });\n\n    await PUT(req, { params: { id: '1' } });\n\n    expect(prisma.post.update).toHaveBeenCalledWith({\n      where: { id: '1' },\n      data: { published: true },\n    });\n  });\n});\n","tests/blogPost.test.tsx":"import { render, screen } from '@testing-library/react';\nimport BlogPost from '../app/blog/[slug]/page';\nimport { useRouter } from 'next/navigation';\n\njest.mock('next/navigation', () => ({ useRouter: jest.fn() }));\njest.mock('@clerk/nextjs', () => ({ ClerkProvider: ({ children }) => children }));\n\ndescribe('BlogPost', () => {\n  beforeEach(() => {\n    (useRouter as jest.Mock).mockReturnValue({ query: { slug: 'test-slug' } });\n  });\n\n  it('renders loading initially', () => {\n    render(<BlogPost />);\n    expect(screen.getByText('Loading...')).toBeInTheDocument();\n  });\n});\n","tests/dashboard.test.tsx":"import { render, screen } from '@testing-library/react';\nimport Dashboard from '../app/dashboard/page';\nimport { useAuth } from '@clerk/nextjs';\n\njest.mock('@clerk/nextjs', () => ({ useAuth: jest.fn() }));\njest.mock('@clerk/nextjs', () => ({ ClerkProvider: ({ children }) => children }));\n\ndescribe('Dashboard', () => {\n  beforeEach(() => {\n    (useAuth as jest.Mock).mockReturnValue({ user: null });\n  });\n\n  it('renders correctly', () => {\n    render(<Dashboard />);\n    expect(screen.getByText('Tableau de bord')).toBeInTheDocument();\n  });\n});\n","tests/layout.test.tsx":"import { render } from '@testing-library/react';\nimport RootLayout from '../app/layout';\n\njest.mock('@clerk/nextjs', () => ({ ClerkProvider: ({ children }) => children }));\n\ndescribe('RootLayout', () => {\n  it('renders children correctly', () => {\n    const { getByText } = render(<RootLayout><div>Test Child</div></RootLayout>);\n    expect(getByText('Test Child')).toBeInTheDocument();\n  });\n});\n","tests/middleware.test.ts":"/**\n * Tests structurels du middleware Clerk v6.\n *\n * Le middleware s'exécute dans le Edge Runtime de Next.js — il n'est pas\n * unit-testable avec jest/jsdom (NextRequest, NextResponse, crypto.subtle…).\n * Ce fichier valide UNIQUEMENT les exports (structure), pas le comportement runtime.\n */\n\n// jest.mock est hoissté avant les imports par le transform Babel/ts-jest.\njest.mock('@clerk/nextjs/server', () => ({\n  clerkMiddleware: jest.fn((handler) => jest.fn()),\n  createRouteMatcher: jest.fn(() => jest.fn(() => false)),\n}));\n\ndescribe('middleware — exports structurels', () => {\n  // eslint-disable-next-line @typescript-eslint/no-var-requires\n  const mod = require('../middleware');\n  const middleware = mod.default ?? mod;\n\n  it('exporte une fonction middleware par défaut', () => {\n    expect(typeof middleware).toBe('function');\n  });\n\n  it('exporte un objet config avec un tableau matcher', () => {\n    expect(mod.config).toBeDefined();\n    expect(Array.isArray(mod.config.matcher)).toBe(true);\n    expect(mod.config.matcher.length).toBeGreaterThan(0);\n  });\n});\n","tests/page.test.tsx":"import { render, screen } from '@testing-library/react';\nimport HomePage from '../app/page';\nimport { useAuth } from '@clerk/nextjs';\n\njest.mock('@clerk/nextjs', () => ({ useAuth: jest.fn() }));\njest.mock('@clerk/nextjs', () => ({ ClerkProvider: ({ children }) => children }));\n\ndescribe('HomePage', () => {\n  beforeEach(() => {\n    (useAuth as jest.Mock).mockReturnValue({ user: null });\n  });\n\n  it('renders correctly', () => {\n    render(<HomePage />);\n    expect(screen.getByText('Articles publiés')).toBeInTheDocument();\n  });\n});\n","tsconfig.json":"{\n  \"compilerOptions\": {\n    \"target\": \"es2017\",\n    \"lib\": [\"dom\", \"dom.iterable\", \"esnext\"],\n    \"allowJs\": true,\n    \"skipLibCheck\": true,\n    \"strict\": true,\n    \"noEmit\": true,\n    \"esModuleInterop\": true,\n    \"module\": \"esnext\",\n    \"moduleResolution\": \"bundler\",\n    \"resolveJsonModule\": true,\n    \"isolatedModules\": true,\n    \"jsx\": \"preserve\",\n    \"incremental\": true,\n    \"plugins\": [{ \"name\": \"next\" }],\n    \"paths\": {\n      \"@/*\": [\"./*\"]\n    }\n  },\n  \"include\": [\"next-env.d.ts\", \"**/*.ts\", \"**/*.tsx\", \".next/types/**/*.ts\"],\n  \"exclude\": [\"node_modules\", \"tests/**\"]\n}\n"},"dev_output":{"files":{".env.local":"NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_placeholder\nCLERK_SECRET_KEY=sk_test_placeholder\nDATABASE_URL=postgresql://user:password@localhost:5432/personal-blog\n","app/api/posts/[id]/route.ts":"import { NextResponse } from 'next/server';\nimport { auth } from '@clerk/nextjs/server';\nimport prisma from '@/lib/prisma';\n\nexport async function PUT(request: Request, { params }: { params: { id: string } }) {\n  const { id } = params;\n  const { published } = await request.json();\n\n  const { userId } = await await auth.protect();\n\n  const post = await prisma.post.update({\n    where: { id },\n    data: { published },\n  });\n\n  return NextResponse.json(post);\n}","app/blog/[slug]/page.tsx":"import { useEffect, useState } from 'react';\nimport { useRouter } from 'next/navigation';\n\ninterface Post {\n  id: string;\n  title: string;\n  content: string;\n  slug: string;\n  published: boolean;\n  authorId: string;\n  createdAt: Date;\n  updatedAt: Date;\n} \n\nconst BlogPost = () => {\n  const router = useRouter();\n  const { slug } = router.query;\n  const [post, setPost] = useState<Post | null>(null);\n\n  useEffect(() => {\n    const fetchPost = async () => {\n      if (slug) {\n        const response = await fetch(`/api/posts/${slug}`);\n        const data = await response.json();\n        setPost(data);\n      }\n    };\n    fetchPost();\n  }, [slug]);\n\n  if (!post) return <div>Loading...</div>;\n\n  return (\n    <div>\n      <h1>{post.title}</h1>\n      <p>{post.content}</p>\n    </div>\n  );\n};\n\nexport default BlogPost;","app/dashboard/page.tsx":"import { useEffect, useState } from 'react';\nimport { useAuth } from '@clerk/nextjs';\nimport { useRouter } from 'next/navigation';\n\ninterface Post {\n  id: string;\n  title: string;\n  slug: string;\n  published: boolean;\n  authorId: string;\n  createdAt: Date;\n  updatedAt: Date;\n} \n\nconst Dashboard = () => {\n  const { user } = useAuth();\n  const [posts, setPosts] = useState<Post[]>([]);\n  const router = useRouter();\n\n  useEffect(() => {\n    const fetchPosts = async () => {\n      const response = await fetch('/api/posts');\n      const data = await response.json();\n      setPosts(data);\n    };\n    fetchPosts();\n  }, []);\n\n  const togglePublished = async (id: string) => {\n    await fetch(`/api/posts/${id}`, {\n      method: 'PUT',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ published: !posts.find(post => post.id === id)?.published })\n    });\n    router.reload();\n  };\n\n  return (\n    <div>\n      <h1>Tableau de bord</h1>\n      <ul>\n        {posts.map((post) => (\n          <li key={post.id}>\n            <span>{post.title}</span>\n            <button onClick={() => togglePublished(post.id)}>Toggle Published</button>\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n};\n\nexport default Dashboard;","app/layout.tsx":"import { ClerkProvider } from '@clerk/nextjs';\nexport const dynamic = \"force-dynamic\";\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <ClerkProvider>\n      <html lang=\"en\">\n        <body>{children}</body>\n      </html>\n    </ClerkProvider>\n  );\n}","app/page.tsx":"import { useEffect, useState } from 'react';\nimport { useAuth } from '@clerk/nextjs';\n\ninterface Post {\n  id: string;\n  title: string;\n  slug: string;\n  published: boolean;\n  authorId: string;\n  createdAt: Date;\n  updatedAt: Date;\n} \n\nconst HomePage = () => {\n  const { user } = useAuth();\n  const [posts, setPosts] = useState<Post[]>([]);\n\n  useEffect(() => {\n    const fetchPosts = async () => {\n      const response = await fetch('/api/posts');\n      const data = await response.json();\n      setPosts(data);\n    };\n    fetchPosts();\n  }, []);\n\n  return (\n    <div>\n      <h1>Articles publiés</h1>\n      <ul>\n        {posts.map((post) => (\n          <li key={post.id}>\n            <a href={`/blog/${post.slug}`}>{post.title}</a>\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n};\n\nexport default HomePage;","jest.config.js":"const nextJest = require('next/jest');\nconst createJestConfig = nextJest({ dir: './' });\n\nmodule.exports = createJestConfig({\n  testEnvironment: 'jsdom',\n  setupFilesAfterEnv: ['<rootDir>/jest.setup.js'],\n  moduleNameMapper: {\n    '^@/(.*)$': '<rootDir>/$1',\n  },\n});\n","jest.setup.js":"import '@testing-library/jest-dom';\n","lib/prisma.ts":"import { PrismaClient } from '@prisma/client';\n\nconst prisma = new PrismaClient();\n\nexport { prisma };\nexport default prisma;\n","middleware.ts":"import { clerkMiddleware, createRouteMatcher } from '@clerk/nextjs/server';\n\nconst isPublicRoute = createRouteMatcher(['/sign-in(.*)', '/sign-up(.*)']);\n\nexport default clerkMiddleware(async (auth, req) => {\n  if (!isPublicRoute(req)) await auth.protect();\n});\n\nexport const config = {\n  matcher: [\n    '/((?!_next|[^?]*\\\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)',\n    '/(api|trpc)(.*)',\n  ],\n};\n","next.config.js":"/** @type {import('next').NextConfig} */\nconst nextConfig = {\n  reactStrictMode: true,\n  eslint: {\n    ignoreDuringBuilds: true,\n  },\n  typescript: {\n    ignoreBuildErrors: false,\n  },\n};\nmodule.exports = nextConfig;\n","package.json":"{\n  \"name\": \"personal-blog\",\n  \"version\": \"1.0.0\",\n  \"private\": true,\n  \"scripts\": {\n    \"build\": \"next build\",\n    \"dev\": \"next dev\",\n    \"start\": \"next start\",\n    \"lint\": \"next lint\",\n    \"test\": \"jest\"\n  },\n  \"dependencies\": {\n    \"next\": \"14.2.25\",\n    \"react\": \"^18.2.0\",\n    \"react-dom\": \"^18.2.0\",\n    \"@clerk/nextjs\": \"^6.0.0\",\n    \"prisma\": \"^7.0.0\",\n    \"@prisma/client\": \"^7.0.0\",\n    \"typescript\": \"^5.3.3\"\n  },\n  \"devDependencies\": {\n    \"ts-jest\": \"29.1.2\",\n    \"@types/react\": \"^18.2.0\",\n    \"@types/react-dom\": \"^18.2.0\",\n    \"@types/jest\": \"^29.0.0\",\n    \"@types/node\": \"^20.0.0\",\n    \"jest\": \"^29.0.0\",\n    \"jest-environment-jsdom\": \"^29.0.0\",\n    \"@testing-library/jest-dom\": \"^6.0.0\",\n    \"@testing-library/react\": \"^14.0.0\",\n    \"@babel/runtime\": \"^7.0.0\",\n    \"node-mocks-http\": \"^1.14.0\",\n    \"eslint\": \"^8.0.0\",\n    \"eslint-config-next\": \"14.2.25\"\n  }\n}","prisma.config.ts":"import 'dotenv/config';\nimport { defineConfig, env } from 'prisma/config';\n\nexport default defineConfig({\n  schema: 'prisma/schema.prisma',\n  datasource: { url: env('DATABASE_URL') },\n});","prisma/schema.prisma":"datasource db {\\n  provider = \"postgresql\"\\n  url      = env(\"DATABASE_URL\")\\n}\\n\\ngenerator client {\\n  provider = \"prisma-client-js\"\\n}\\n\\nmodel Post {\\n  id        String   @id @default(cuid())\\n  title     String\\n  content   String\\n  slug      String   @unique\\n  published Boolean  @default(false)\\n  authorId  String\\n  createdAt DateTime @default(now())\\n  updatedAt DateTime @updatedAt\\n}","tests/middleware.test.ts":"/**\n * Tests structurels du middleware Clerk v6.\n *\n * Le middleware s'exécute dans le Edge Runtime de Next.js — il n'est pas\n * unit-testable avec jest/jsdom (NextRequest, NextResponse, crypto.subtle…).\n * Ce fichier valide UNIQUEMENT les exports (structure), pas le comportement runtime.\n */\n\n// jest.mock est hoissté avant les imports par le transform Babel/ts-jest.\njest.mock('@clerk/nextjs/server', () => ({\n  clerkMiddleware: jest.fn((handler) => jest.fn()),\n  createRouteMatcher: jest.fn(() => jest.fn(() => false)),\n}));\n\ndescribe('middleware — exports structurels', () => {\n  // eslint-disable-next-line @typescript-eslint/no-var-requires\n  const mod = require('../middleware');\n  const middleware = mod.default ?? mod;\n\n  it('exporte une fonction middleware par défaut', () => {\n    expect(typeof middleware).toBe('function');\n  });\n\n  it('exporte un objet config avec un tableau matcher', () => {\n    expect(mod.config).toBeDefined();\n    expect(Array.isArray(mod.config.matcher)).toBe(true);\n    expect(mod.config.matcher.length).toBeGreaterThan(0);\n  });\n});\n","tsconfig.json":"{\n  \"compilerOptions\": {\n    \"target\": \"es2017\",\n    \"lib\": [\"dom\", \"dom.iterable\", \"esnext\"],\n    \"allowJs\": true,\n    \"skipLibCheck\": true,\n    \"strict\": true,\n    \"noEmit\": true,\n    \"esModuleInterop\": true,\n    \"module\": \"esnext\",\n    \"moduleResolution\": \"bundler\",\n    \"resolveJsonModule\": true,\n    \"isolatedModules\": true,\n    \"jsx\": \"preserve\",\n    \"incremental\": true,\n    \"plugins\": [{ \"name\": \"next\" }],\n    \"paths\": {\n      \"@/*\": [\"./*\"]\n    }\n  },\n  \"include\": [\"next-env.d.ts\", \"**/*.ts\", \"**/*.tsx\", \".next/types/**/*.ts\"],\n  \"exclude\": [\"node_modules\", \"tests/**\"]\n}\n"},"final_message":"BuildNotAttempted: pre-build gate bloque. REQUIREMENTS GATE — BUILD BLOQUÉ\n1 requirement(s) métier mappable(s) non couverts :\n  - Modèle Prisma: Post avec champs title, content, slug @unique, published Boolean, authorId String\n\nGénère les fichiers manquants avant d'appeler run_build.","metadata":{"build_attempted":false,"build_attempts":0,"iterations":14,"last_build_error":"","last_build_error_full":"","last_failed_command":"","last_test_error":"","last_test_error_full":"","total_files":16},"success":false},"metadata":{"dev_files_count":16,"mode":"fusion","requirements_met":4,"requirements_total":6,"requirements_unmet":["Modèle Prisma: Post avec champs title, content, slug @unique, published Boolean, authorId String","Feature: slug généré côté serveur depuis title"],"requirements_unmet_by_category":{},"spec_coverage":0.667,"spec_unmatched_count":0,"spec_validation_status":"OK","test_files_count":5,"tests_passed":false,"total_files":21},"run_metric":{"build_attempted":false,"build_attempts":0,"build_success":false,"clerk_compliant":true,"dev_files_count":16,"error":"BuildFailed: BuildNotAttempted: pre-build gate bloque. REQUIREMENTS GATE — BUILD BLOQUÉ\n1 requirement(s) métier mappable(s) non couverts :\n  - Modèle Prisma: Post avec champs title, content, slug @unique, publishe","files_count":21,"final_message":"BuildNotAttempted: pre-build gate bloque. REQUIREMENTS GATE — BUILD BLOQUÉ\n1 requirement(s) métier mappable(s) non couverts :\n  - Modèle Prisma: Post avec champs title, content, slug @unique, publishe","iterations":14,"last_build_error":"","last_build_error_full":"","last_failed_command":"","last_test_error":"","last_test_error_full":"","root_cause_category":"max_iterations","semantic_violations":[]},"semantic_violations":[],"success":false,"test_output":{"success":false,"tests":{"tests/api/posts.test.ts":"import { NextResponse } from 'next/server';\nimport { PUT } from '../app/api/posts/[id]/route';\nimport { auth } from '@clerk/nextjs/server';\nimport prisma from '@/lib/prisma';\nimport httpMocks from 'node-mocks-http';\n\njest.mock('@clerk/nextjs/server', () => ({ auth: { protect: jest.fn() } }));\njest.mock('@/lib/prisma', () => ({ post: { update: jest.fn() } }));\n\ndescribe('PUT /api/posts/[id]', () => {\n  it('updates a post', async () => {\n    const req = httpMocks.createRequest({\n      method: 'PUT',\n      body: { published: true },\n      params: { id: '1' },\n    });\n    const res = httpMocks.createResponse();\n\n    (auth.protect as jest.Mock).mockResolvedValue({ userId: 'user-id' });\n    (prisma.post.update as jest.Mock).mockResolvedValue({ id: '1', published: true });\n\n    await PUT(req, { params: { id: '1' } });\n\n    expect(prisma.post.update).toHaveBeenCalledWith({\n      where: { id: '1' },\n      data: { published: true },\n    });\n  });\n});\n","tests/blogPost.test.tsx":"import { render, screen } from '@testing-library/react';\nimport BlogPost from '../app/blog/[slug]/page';\nimport { useRouter } from 'next/navigation';\n\njest.mock('next/navigation', () => ({ useRouter: jest.fn() }));\njest.mock('@clerk/nextjs', () => ({ ClerkProvider: ({ children }) => children }));\n\ndescribe('BlogPost', () => {\n  beforeEach(() => {\n    (useRouter as jest.Mock).mockReturnValue({ query: { slug: 'test-slug' } });\n  });\n\n  it('renders loading initially', () => {\n    render(<BlogPost />);\n    expect(screen.getByText('Loading...')).toBeInTheDocument();\n  });\n});\n","tests/dashboard.test.tsx":"import { render, screen } from '@testing-library/react';\nimport Dashboard from '../app/dashboard/page';\nimport { useAuth } from '@clerk/nextjs';\n\njest.mock('@clerk/nextjs', () => ({ useAuth: jest.fn() }));\njest.mock('@clerk/nextjs', () => ({ ClerkProvider: ({ children }) => children }));\n\ndescribe('Dashboard', () => {\n  beforeEach(() => {\n    (useAuth as jest.Mock).mockReturnValue({ user: null });\n  });\n\n  it('renders correctly', () => {\n    render(<Dashboard />);\n    expect(screen.getByText('Tableau de bord')).toBeInTheDocument();\n  });\n});\n","tests/layout.test.tsx":"import { render } from '@testing-library/react';\nimport RootLayout from '../app/layout';\n\njest.mock('@clerk/nextjs', () => ({ ClerkProvider: ({ children }) => children }));\n\ndescribe('RootLayout', () => {\n  it('renders children correctly', () => {\n    const { getByText } = render(<RootLayout><div>Test Child</div></RootLayout>);\n    expect(getByText('Test Child')).toBeInTheDocument();\n  });\n});\n","tests/page.test.tsx":"import { render, screen } from '@testing-library/react';\nimport HomePage from '../app/page';\nimport { useAuth } from '@clerk/nextjs';\n\njest.mock('@clerk/nextjs', () => ({ useAuth: jest.fn() }));\njest.mock('@clerk/nextjs', () => ({ ClerkProvider: ({ children }) => children }));\n\ndescribe('HomePage', () => {\n  beforeEach(() => {\n    (useAuth as jest.Mock).mockReturnValue({ user: null });\n  });\n\n  it('renders correctly', () => {\n    render(<HomePage />);\n    expect(screen.getByText('Articles publiés')).toBeInTheDocument();\n  });\n});\n"}}}

{"e2e_tests":{"tests/e2e/generated_e2e.spec.ts":"import { test, expect } from '@playwright/test';\n\nconst BASE_URL = process.env.BASE_URL || 'http://localhost:3000';\n\ntest.describe('Personal Blog E2E Tests', () => {\n  \n  test('Navigation to sign-in page', async ({ page }) => {\n    await page.goto(BASE_URL);\n    await page.click('text=Se connecter');\n    await expect(page).toHaveURL(`${BASE_URL}/sign-in`);\n  });\n\n  test('Successful authentication redirects to dashboard', async ({ page }) => {\n    await page.goto(`${BASE_URL}/sign-in`);\n    await page.fill('input[name=\"email\"]', 'test@example.com');\n    await page.fill('input[name=\"password\"]', 'password123');\n    await page.click('button[type=\"submit\"]');\n    await expect(page).toHaveURL(`${BASE_URL}/dashboard`);\n  });\n\n  test('Display published articles on home page', async ({ page }) => {\n    await page.goto(BASE_URL);\n    const articles = await page.locator('h2'); // Assuming articles are in h2 tags\n    await expect(articles).toHaveCount(3); // Adjust count based on expected articles\n  });\n\n  test('View specific blog post', async ({ page }) => {\n    await page.goto(`${BASE_URL}/blog/sample-slug`); // Replace with a valid slug\n    await expect(page.locator('h1')).toHaveText('Titre de l\\'article'); // Replace with expected title\n  });\n\n  test('Dashboard displays user posts', async ({ page }) => {\n    await page.goto(`${BASE_URL}/dashboard`);\n    const posts = await page.locator('.post-item'); // Assuming posts have a class of post-item\n    await expect(posts).toHaveCount(2); // Adjust count based on expected posts\n  });\n\n  test('Error handling on invalid post update', async ({ page }) => {\n    await page.goto(`${BASE_URL}/dashboard`);\n    await page.click('text=Editer un article'); // Assuming there's a button to edit\n    await page.fill('input[name=\"title\"]', ''); // Leave title empty to trigger error\n    await page.click('button[type=\"submit\"]');\n    await expect(page.locator('.error-message')).toHaveText('Le titre est requis'); // Adjust based on actual error message\n  });\n\n});"}}

{"e2e_tests":{"tests/e2e/generated_e2e.spec.ts":"import { test, expect } from '@playwright/test';\n\nconst BASE_URL = process.env.BASE_URL || 'http://localhost:3000';\n\ntest.describe('Personal Blog E2E Tests', () => {\n  \n  test('Navigation to sign-in page', async ({ page }) => {\n    await page.goto(BASE_URL);\n    await page.click('text=Se connecter');\n    await expect(page).toHaveURL(`${BASE_URL}/sign-in`);\n  });\n\n  test('Successful authentication redirects to dashboard', async ({ page }) => {\n    await page.goto(`${BASE_URL}/sign-in`);\n    await page.fill('input[name=\"email\"]', 'test@example.com');\n    await page.fill('input[name=\"password\"]', 'password123');\n    await page.click('button[type=\"submit\"]');\n    await expect(page).toHaveURL(`${BASE_URL}/dashboard`);\n  });\n\n  test('Display published articles on home page', async ({ page }) => {\n    await page.goto(BASE_URL);\n    const articles = await page.locator('h2'); // Assuming articles are in h2 tags\n    await expect(articles).toHaveCount(3); // Adjust count based on expected articles\n  });\n\n  test('View specific blog post', async ({ page }) => {\n    await page.goto(`${BASE_URL}/blog/sample-slug`); // Replace with a valid slug\n    await expect(page.locator('h1')).toHaveText('Titre de l\\'article'); // Replace with expected title\n  });\n\n  test('Dashboard displays user posts', async ({ page }) => {\n    await page.goto(`${BASE_URL}/dashboard`);\n    const posts = await page.locator('.post-item'); // Assuming posts have a class of post-item\n    await expect(posts).toHaveCount(2); // Adjust count based on expected posts\n  });\n\n  test('Error handling on invalid post update', async ({ page }) => {\n    await page.goto(`${BASE_URL}/dashboard`);\n    await page.click('text=Editer un article'); // Assuming there's a button to edit\n    await page.fill('input[name=\"title\"]', ''); // Leave title empty to trigger error\n    await page.click('button[type=\"submit\"]');\n    await expect(page.locator('.error-message')).toHaveText('Le titre est requis'); // Adjust based on actual error message\n  });\n\n});"}}