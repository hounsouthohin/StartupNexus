# SUPPRIMÉ — 28 Mars 2026
# Ce fichier est mort. La logique de parsing déterministe est remplacée par un
# brief structuré JSON (description, models[], pages[], routes[]) lu directement
# dans planner_node de architect.py — zéro regex, zéro ambiguïté multi-modèles.
raise ImportError(
    "brief_parser.py est supprimé. Utiliser le brief structuré JSON à la place."
)
