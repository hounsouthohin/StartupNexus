# SUPPRIMÉ — 28 Mars 2026
# Tests du brief_parser déterministe, lui-même supprimé.
# La source de vérité est désormais le brief structuré JSON (brief_catalog.py).
import pytest
pytestmark = pytest.mark.skip(reason="brief_parser supprimé — remplacé par brief structuré JSON")
